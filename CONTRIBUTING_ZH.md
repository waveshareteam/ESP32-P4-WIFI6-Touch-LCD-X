# 为 ESP32-P4-WIFI6-Touch-LCD-X 仓库贡献

[English](CONTRIBUTING.md)

感谢您对本仓库的关注。本仓库包含微雪 ESP32-P4-WIFI6-Touch-LCD-X 系列的第一方
ESP-IDF 示例、出厂固件和开发板文档。

## 提交问题

提交问题时请包含：

- 产品型号和硬件版本（如已知）
- 示例路径和 ESP-IDF 版本
- 复现步骤、预期行为和实际行为
- 构建/烧录日志和串口输出
- 问题出现在源码构建示例还是出厂固件上

## 提交 Pull Request

- 保持改动范围聚焦，并在描述中说明动机。
- 行为或配置发生变化时，同步更新受影响的示例文档（英文和简体中文）。
- 合入前，请确保每个受影响的 ESP-IDF 示例都在 ESP-IDF `v5.5.5` 和 `v6.0.2`
  下针对 `esp32p4` 验证通过。
- 除非是明确的发布更新，否则不要新增、删除或重新打包出厂固件和交付压缩包。
- 仓库公开文本中不要包含本地路径、凭据或环境相关的私有信息。

## CI

仓库 CI 会在两条支持的 ESP-IDF 版本线上构建受影响的示例，并校验第一方
Markdown 链接。合入前必须通过 CI。
