dependencies:
  idf: ">=5.4"

  waveshare/esp32_p4_wifi6_touch_lcd_x: "^2.0.3"

  espressif/esp_audio_codec:
    # Keep the CI-validated 2.5.x API contract. Re-evaluate later releases
    # only after both supported ESP-IDF lines pass for this example.
    version: "2.5.*"
    public: true
